# ENV Var Confessional

A simple tool that checks if your environment variables match what's expected. Run: `node env-confessional.js [.env.example]` to see which variables are missing or empty. No more debugging mysterious crashes!